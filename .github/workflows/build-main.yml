name: Build and Publish Release

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - uses: alterioncorp/.github/.github/actions/import-gpg-key@main
        with:
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}

      - uses: alterioncorp/.github/.github/actions/maven-settings@main

      - name: Build and Publish
        run: mvn clean deploy -U -Ppublish
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_TOKEN: ${{ secrets.CENTRAL_TOKEN }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: Save coverage report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco/jacoco.xml
          retention-days: 1

  upload-coverage:
    needs: build-and-publish
    uses: alterioncorp/.github/.github/workflows/upload-coverage.yml@main
    secrets:
      codecov-token: ${{ secrets.CODECOV_TOKEN }}
